import styled from "styled-components";

export const Content = styled.div`
  width: 400px;

  button {
    margin-top: 1.5rem;
  }

  span {
    color: var(--second-color);
    cursor: pointer;
  }
`;
