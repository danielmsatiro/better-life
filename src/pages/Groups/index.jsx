// import { Container } from './styles';
import { ListMiniCards } from "../../components/ListMiniCards";

const Groups = () => {
  return (
    <div>
      <ListMiniCards />
    </div>
  );
};

export default Groups;
